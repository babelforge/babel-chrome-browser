User:
  type: object
  required:
    - id
    - name
  properties:
    id:
      type: integer
      format: int64
      example: 42
    name:
      type: string
      example: Ada Lovelace
    email:
      type: string
      format: email
      example: ada@example.test
